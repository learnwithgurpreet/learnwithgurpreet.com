#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../liquidjs/bin/liquid.js" "$@"
else
  exec node  "$basedir/../../../../liquidjs/bin/liquid.js" "$@"
fi
# cmd-shim-target=/Users/gursingh7/codebase/personal/git/learnwithgurpreet.com/node_modules/.pnpm/@11ty+eleventy@3.1.6/node_modules/liquidjs/bin/liquid.js
