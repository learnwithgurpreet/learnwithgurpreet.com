#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../acorn/bin/acorn" "$@"
else
  exec node  "$basedir/../../../../acorn/bin/acorn" "$@"
fi
# cmd-shim-target=/Users/gursingh7/codebase/personal/git/learnwithgurpreet.com/node_modules/.pnpm/@11ty+dependency-tree-esm@2.0.4/node_modules/acorn/bin/acorn
