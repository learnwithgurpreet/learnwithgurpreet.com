<header>
  <div class="header-section">
    <div class="header-frame">
      <h1>{{ metadata.author.name }}</h1>
      <h2>{{ metadata.author.subtitle }}</h2>
    </div>
    <div class="img-frame"><img src="{{ authorImage }}" alt="{{ metadata.author.name }}"/>
    </div>
  </div>
</header>